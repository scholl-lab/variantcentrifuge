repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-json
    -   id: check-toml

-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0
    hooks:
    -   id: ruff
        args: [--fix]
    -   id: ruff-format

-   repo: local
    hooks:
    -   id: python-module-line-budget
        name: Enforce production Python module line budget
        entry: python scripts/check_file_size.py variantcentrifuge --allowlist .loc-allowlist
        language: system
        pass_filenames: false
        types: [python]
    -   id: pandas-groupby-observed
        name: Enforce observed=True in groupby
        entry: bash -c 'if grep -rn "\.groupby(" --include="*.py" variantcentrifuge/ scripts/ | grep -v "observed=True" | grep -v "^Binary"; then echo "ERROR - groupby() calls must include observed=True"; exit 1; fi'
        language: system
        pass_filenames: false
        types: [python]
